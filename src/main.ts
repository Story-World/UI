import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';

import { storyWorldModule } from './app/components/app.module';

platformBrowserDynamic().bootstrapModule(storyWorldModule);
